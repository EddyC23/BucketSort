#pragma once
#include "VortexS.h"
#include <vector>
class IntervalTree {
	//need to actually implement the interval tree instead
	//mask and shift?
	//is this actallyl needed just shift?
public:
	IntervalTree();

private:
	std::vector<VortexS> vortexes;
	
};
	